
import React from 'react'
import ReactDOM from 'react-dom/client'
import PixSimulator from './PixSimulator'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <PixSimulator />
  </React.StrictMode>,
)
